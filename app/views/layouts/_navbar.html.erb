<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><i class="fab fa-blogger fa-lg"></i> <tab> LOG</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item <%= 'active' if current_page?(root_path) %>">
          <a class="nav-link" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item <%= 'active' if current_page?(pages_about_path) %>">
          <%= link_to "About", pages_about_path, class:"nav-link"%>
        </li>
      </ul>
        <ul class="navbar-nav ml-auto">
          <% if current_user %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.username%>
            </a>
            <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <li>
                <a class="dropdown-item" href="#">Action</a>
              </li>
              <li>
                <a class="dropdown-item" href="<%= edit_user_registration_path%>">Account Settings</a>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item", data: {confirm: "Are you sure? "}%>
              </li>
            </ul>
          </li>
          <li class="nav-item <%= 'active' if current_page?(new_post_path) %>">
            <a class="nav-link" aria-current="page" href="<%= new_post_path %>">Add Blog</a>
          </li>
        </ul>
      <% else %>
      <li class="nav-item <%= 'active' if current_page?(new_user_registration_path) %>">
        <a class="nav-link" aria-current="page" href="<%= new_user_registration_path %>">Sign Up</a>
      </li>
      <li class="nav-item <%= 'active' if current_page?(new_user_session_path) %>">
        <a class="nav-link" aria-current="page" href="<%= new_user_session_path %>">Sign In</a>
      </li>
      <% end%>
    </div>
  </div>
</nav>


<style>
  .navbar-nav > .active > a {
    border-bottom: 2px solid red;
  }
</style>
